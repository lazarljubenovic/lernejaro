/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LinePipe } from './line.pipe';

describe('LinePipe', () => {
  it('create an instance', () => {
    let pipe = new LinePipe();
    expect(pipe).toBeTruthy();
  });
});
