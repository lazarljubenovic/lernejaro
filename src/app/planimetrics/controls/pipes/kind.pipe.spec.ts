/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { KindPipe } from './kind.pipe';

describe('KindPipe', () => {
  it('create an instance', () => {
    let pipe = new KindPipe();
    expect(pipe).toBeTruthy();
  });
});
